/* Bucket Golf — Supabase config
 *
 * Copy this file to `config.js` and fill in the values from your Supabase
 * project (Project Settings -> API). `config.js` is gitignored so your keys
 * never end up in source control.
 *
 * If `config.js` is missing OR the values below are still the placeholders,
 * the app falls back to localStorage so it keeps working offline.
 */
window.SUPABASE_CONFIG = {
  url: "https://YOUR-PROJECT-REF.supabase.co",
  anonKey: "YOUR-PUBLIC-ANON-KEY",
};
